:{$PORT} {
	root ./
    encode zstd gzip        # 压缩资源文件 加快访问速度
    file_server browse

	route /admin/* {
        respond "I am admin"
    }

    route /google/* {
        # redir https://{host}
        # uri strip_prefix /google
        uri replace /google/ //
        reverse_proxy * https://www.google.com {
            header_up Host {upstream_hostport}
        }
    }
}

# :{$PORT} {
# 	reverse_proxy * {$UPSTREAM:"https://www.google.com"} {
# 		header_up Host {upstream_hostport}
# 	}
# }

# https://sayonara3.herokuapp.com {
#     respond "I am admin"
# }